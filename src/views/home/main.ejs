<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>UNIV_MEETING_PLACE</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
    <link rel="stylesheet" href="/css/home/main.css" />
    <script
      src="https://code.jquery.com/jquery-3.6.0.js"
      integrity="sha256-H+K7U5CnXl1h5ywQfKtSj8PCmoN9aaq30gDh27Xc0jk="
      crossorigin="anonymous"
    ></script>
    <script type="text/javascript"></script>
    <script src="/js/home/main.js" defer></script>
  </head>

  <body>
    <div class="Mainpage">
      <div id="wrap">
        <h1>
          <span class="material-icons">travel_explore</span>
        </h1>
        <p id="dynamic" class="lg-text"></p>
      </div>
      <% if (is_logined) {%>
      <button type="button" class="mainpage-button button1" onclick="location.href='/logout'">로그아웃</button
      ><% } else { %>
      <button type="button" class="mainpage-button button1" onclick="location.href='/login'">로그인</button>
      <% } %>
      <button type="button" class="mainpage-button button2" onclick=<% if (is_logined) {%>
        "goIndex();" <% } else { %> "info();" <% } %>>새로운 약속
      </button>
      <button type="button" class="mainpage-button button3" onclick=<% if (is_logined) {%>
        "goList();" <% } else { %> "info();" <% } %>>약속 확인
      </button>
    </div>
  </body>
</html>
